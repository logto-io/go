# golangci-lint v2 configuration.
# The exclusion presets below restore the default exclusions of golangci-lint v1,
# matching the behavior generated by `golangci-lint migrate`.
version: "2"

linters:
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
